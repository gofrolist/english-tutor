"""Entry point for running the scheduler as a module.

Run via: python -m english_tutor.scheduler
"""

from src.english_tutor.scheduler import run_scheduler

if __name__ == "__main__":
    run_scheduler()
